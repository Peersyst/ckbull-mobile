import styled from "@peersyst/react-native-styled";
import { Row } from "@peersyst/react-native-components";

export const PendingListSection = styled(Row)(() => ({
    marginVertical: 18,
    justifyContent: "flex-start",
}));
